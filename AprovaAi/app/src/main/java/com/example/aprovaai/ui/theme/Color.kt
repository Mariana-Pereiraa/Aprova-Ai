package com.example.aprovaai.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais
val BlueLight = Color(0xFFD1F1F8) // Fundo azul claro
val BlueBase = Color(0xFF87CFEA)  // Tons dos botões
val BlueDark = Color(0xFF4A9DB1)  // Versão mais escura do azul

// Cores secundárias
val GrayLight = Color(0xFFEAEDED) // Cinza claro para superfícies
val GrayDark = Color(0xFF2C3E50)  // Cinza escuro para textos e ícones

// Cores de destaque (ex.: ícones ou interação)
val RedHighlight = Color(0xFFF94144) // Destaque em vermelho

// Cores adicionais
val BlackText = Color(0xFF000000) // Texto preto
